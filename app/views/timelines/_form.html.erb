
<% if user_signed_in? %>
  <%= form_for(@timeline) do |f| %>
    <% if @timeline.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@timeline.errors.count, "error") %> prohibited this timeline from being saved:</h2>

        <ul>
        <% @timeline.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <!-- <div class="field">
      <%= f.label :user_id %><br>
      <%= f.number_field :user_id %>
    </div> -->
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

<% else %>
  <a href="<%= new_user_session_path %>">sign in to create a timeline</a>
<% end %>
